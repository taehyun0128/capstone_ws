name: Rolling
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build_ros_rolling:
    uses: ./.github/workflows/ros_build_reusable.yaml
    with:
      ros_distro: rolling
      container_image: osrf/ros:rolling-desktop
